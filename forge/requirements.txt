# ── Phase 1: run the agent + benchmarks (only dependency you need today) ──────
requests>=2.31.0

# ── Phase 3: semantic memory (install when you reach it) ──────────────────────
# chromadb>=0.5.0
# sentence-transformers>=3.0.0

# ── Phase 6: local fine-tuning on your RTX 4070 (QLoRA, 12GB-friendly) ─────────
# torch>=2.3.0
# transformers>=4.44.0
# datasets>=2.20.0
# peft>=0.12.0
# trl>=0.9.0
# bitsandbytes>=0.43.0
# accelerate>=0.31.0
# (Ollama itself is a separate native install: https://ollama.com/download)
